const Home = () => {
    return (
        <div>
            <h1>Task Board</h1>
            <p>Welcome to the Task Board</p>
            <button>Login</button>
            <button>Register</button>
        </div>
    );
};

export default Home;